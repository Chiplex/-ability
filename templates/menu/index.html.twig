{% for item in aside_menus %}
<li class="nav-item">
    <a class="nav-link" href="{{path(item.link)}}">
        <i class="fas fa-fw fa-chart-area"></i>
        <span>{{item.titulo}}</span>
    </a>
</li>
{% endfor %}